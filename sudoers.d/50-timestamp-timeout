# `timestamp_timeout` is the number of minutes that can elapse before
# sudo will ask for a passwd again. The default is 5, which some
# people find a bit short.

Defaults                    timestamp_timeout=15
